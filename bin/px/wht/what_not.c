#include <oraccsys.h>
#include "../px.h"
#include "what.h"

/* This routine is intended for use when the what_xxx is supposed
 * return a simple file or result.  If the result is supposed to be
 * embedded in a pager 
 */

int
what_not(Isp *ip, const char *no) {
  
  return 0;
}
